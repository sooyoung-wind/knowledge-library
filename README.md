# 소소한 지식 저장소

MkDocs와 GitHub Pages로 운영하는 개인 문서 저장소입니다.

## Run

```bash
uv sync
uv run python -m mkdocs serve
```

## Build

```bash
uv run python -m mkdocs build
```

## Deploy

`main` 브랜치에 push 하면 GitHub Pages로 배포됩니다.
